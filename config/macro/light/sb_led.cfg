# SYNC=0: send the update now rather than queueing it against the movement
# queue. See the note in light/chamber_led.cfg.
[gcode_macro SB_LED_ON]
gcode:
  SET_LED LED=sb RED=1 GREEN=1 BLUE=1 SYNC=0

[gcode_macro SB_LED_OFF]
gcode:
  SET_LED LED=sb RED=0 GREEN=0 BLUE=0 SYNC=0