# mainsail.cfg already declares a bare [respond]. This block intentionally
# augments it (Klipper merges duplicate sections) to drop the "echo:" prefix.
# Do not "fix" it by editing mainsail.cfg - that file belongs to the
# mainsail-config repo and update_manager will overwrite it.
[respond]
default_type: echo
default_prefix: